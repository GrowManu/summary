{% extends 'user/user.html.twig' %}
{% block body %}

    <h1>
        {{ title }}
    </h1>

    <a href="{{ path('user_summaries_create') }}">Create User</a><br>

    <h2>Users</h2>

    <ul>
        {% for user in users %}
            <li>{{ user.username }}</li>
        {% endfor %}
    </ul>

{% endblock %}